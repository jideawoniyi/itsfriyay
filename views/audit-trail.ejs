<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <!-- <meta name="viewport" content="width=device-width, initial-scale=1" /> -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="stylesheet" href="/css/style.css">
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://getbootstrap.com/docs/5.3/assets/css/docs.css"
      rel="stylesheet"
    />
    <title>Audit Trail</title>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    
    <script>
      $(document).ready(function() {
      console.log('jQuery is working');
      });
    </script>
  </head>

  <!-- style="background-color: rgb(35, 35, 35); -->
  <body style="box-sizing: border-box">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <a class="navbar-brand" href="#">AUDIT</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#menu" aria-controls="menu" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="menu">
          <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
            <% if (user) { %>
              <li class="nav-item"><a class="nav-link" href="/dashboard">Dashboard</a></li>
              <li class="nav-item"><a class="nav-link" href="/logout/<%= user.username %>">Log out</a></li>
            <% } else { %>
              <li class="nav-item"><a class="nav-link" href="/">Login</a></li>
              <li class="nav-item"><a class="nav-link" href="/register">Register</a></li>
            <%
            } %>
          </ul>
      </div>
    </nav>


    
     
      <main class="p-3 m-0 border-0 bd-example">
        <div>
           <% events.reverse().forEach((event, index) => { %>
            <% let word = index + 1; %>
           
            <div class="accordion .accordion-flush col-lg-12 col-md-12 col-sm-12 col-xs-12" id="accordionPanelsStayOpenExample" style="margin-bottom: 10px;">
              <div class="accordion-item col-lg-12 col-md-12 col-sm-12 col-xs-12">
                <h2 class="accordion-header d-lg-block d-md-block d-sm-none d-none" id="panelsStayOpen-heading<%= word %>">
                  <button
                    class="accordion-button collapsed"
                    type="button"
                    data-bs-toggle="collapse"
                    data-bs-target="#panelsStayOpen-collapse<%= word %>"
                    aria-expanded="false"
                    aria-controls="panelsStayOpen-collapse<%= word %>"
                  >
                    No.<%= index +1 %>
                  </button>
                </h2>
                <h2 class="accordion-header d-sm-block d-md-none d-lg-none d-block" id="panelsStayOpen-heading<%= word %>">
                  <button
                    class="accordion-button collapsed"
                    type="button"
                    data-bs-toggle="collapse"
                    data-bs-target="#panelsStayOpen-collapse<%= word %>"
                    aria-expanded="false"
                    aria-controls="panelsStayOpen-collapse<%= word %>"
                  >
                    No.<%= index +1 %> <span class="accordion-icon" aria-hidden="true"></span>
                  </button>
                </h2>
                <div
                  id="panelsStayOpen-collapse<%= word %>"
                  class="accordion-collapse collapse col-lg-12 col-md-12 col-sm-12 col-xs-12"
                  aria-labelledby="panelsStayOpen-heading<%= word %>"
                >
                  <div class="accordion-body col-lg-12 col-md-12 col-sm-12 col-xs-12">
                    <strong style="font-weight: bold; color: black;">Date: </strong><%= event.date.toLocaleString() %>
                    <strong style="font-weight: bold; color: black;">Description: </strong><%= event.description %>
                    <strong style="font-weight: bold; color: black;">Email: </strong><%= event.email %>
                    <strong style="font-weight: bold; color:black;">Username: </strong><%= event.username %>
                    <strong style="font-weight: bold; color: black;">Activity: </strong><%= event.type %>
                  </div>
                </div>
              </div>
            </div>
        
            
          </div>
          <% }); %>
    
      </main>

      <% console.log(page, totalPages); %>
      <nav aria-label="Page navigation example ">
        <ul class="pagination justify-content-center">
          <% if (page > 1) { %>
            <li class="page-item">
              <a class="page-link" style="color: black" href="/audit-trail/<%= page - 1 %>" aria-label="Previous">
                <span aria-hidden="true">&laquo;</span>
                <span class="sr-only">Previous</span>
              </a>
            </li>
          <% } %>
          <% for (let i = 1; i <= totalPages; i++) { %>
            <li class="page-item"><a class="page-link" style="color: black" href="/audit-trail/<%= i %>"><%= i %></a></li>
          <% } %>
          <% if (page < totalPages) { %>
            <li class="page-item">
              <a class="page-link" style="color: black" href="/audit-trail/<%= page + 1 %>" aria-label="Next">
                <span aria-hidden="true">&raquo;</span>
                <span class="sr-only">Next</span>
              </a>
            </li>
          <% } %>
        </ul>
      </nav>

        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/jqueryui/1.12.1/jquery-ui.min.js"></script>
</body>
</html>